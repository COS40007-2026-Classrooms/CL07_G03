# pinned versions so the pipeline produces consistent results every run

tensorflow==2.15.0   # core deep learning framework - builds and trains the model
numpy==1.26.4        # numerical operations - array handling throughout the pipeline
matplotlib==3.8.4    # plotting - generates loss curves and actual vs predicted plots
pandas==2.2.2        # data handling - loading and cleaning the Tetouan CSV
seaborn==0.13.2      # statistical visualisation - enhanced plot styling
keras==2.15.0        # high-level neural network API - bundled with tensorflow
scikit-learn==1.4.2  # preprocessing - StandardScaler, metrics (RMSE, MAE, R²)
joblib==1.3.2        # serialisation - saving and loading the fitted scaler
scipy==1.12.0        # statistical tests - KS test for concept drift detection
dvc[gdrive]>=3.50.0  # data version control - pipeline management and remote storage