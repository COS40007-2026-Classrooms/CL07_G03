# Models
Store all trained model files here. Name them with the version number and training date, like model_v1.0.pkl. Also keep the model_registry.json file here to track which version is currently active.
